[settings]
paranoid = true
locked_verify_provenance = true
lockfile = true

[tools]
node = "26.2.0"
"aqua:yarnpkg/berry" = "4.15.0"
"aqua:suzuki-shunsuke/pinact" = "4.0.0"

[tasks.pin-actions]
run = "pinact run"
description = "Pin all GitHub Actions to commit SHAs"
